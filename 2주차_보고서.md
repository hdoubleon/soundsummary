# 🎨 강의 녹음 요약 AI - 2주차 개발 보고서

**프로젝트명**: SoundSummary  
**개발 기간**: 2025년 11월 18일  
**개발자**: 이재훈  
**Repository**: hdoubleon/soundsummary  
**Branch**: dev  
**버전**: 2.0.1 → 2.1.0

---

## 📋 목차
1. [개요](#1-개요)
2. [신규 기능](#2-신규-기능)
3. [기술 구현](#3-기술-구현)
4. [코드 변경 사항](#4-코드-변경-사항)
5. [테스트 및 검증](#5-테스트-및-검증)
6. [성과 및 개선점](#6-성과-및-개선점)

---

## 1. 개요

### 1.1 개발 목표
1주차에 완성된 기본 기능에 대해 **UI/UX 개선**에 집중
- 사용자 경험 향상
- 접근성 개선
- 모바일 환경 최적화

### 1.2 주요 업데이트
- ✅ 다크/라이트 테마 전환 시스템
- ✅ 프로그레스 바 및 애니메이션 강화
- ✅ 모바일 터치 제스처 지원
- ✅ CSS 변수 시스템 도입
- ✅ 전체 UI 요소 테마 대응

---

## 2. 신규 기능

### 2.1 다크/라이트 테마 전환 🎨

#### 기능 개요
- 사용자가 원하는 테마 선택 가능
- 다크 테마 (기본) / 라이트 테마
- localStorage에 선호도 저장
- 페이지 재방문 시 자동 적용

#### 구현 위치
```html
<!-- 사이드바 하단에 테마 전환 버튼 추가 -->
<div class="theme-toggle-section">
    <button class="theme-toggle-btn" onclick="toggleTheme()">
        <i class="fas fa-sun theme-icon-light"></i>
        <i class="fas fa-moon theme-icon-dark"></i>
        <span>테마 전환</span>
    </button>
</div>
```

#### 동작 방식
1. 버튼 클릭 → `toggleTheme()` 함수 실행
2. `body` 태그에 `light-theme` 클래스 토글
3. localStorage에 `vscode_lectureAI_theme` 저장
4. CSS 변수 값 자동 변경

#### CSS 변수 시스템
```css
:root {
    /* 다크 테마 (기본) */
    --bg-primary: #1e1e1e;
    --bg-secondary: #252526;
    --bg-tertiary: #2d2d30;
    --text-primary: #cccccc;
    --accent-primary: #007acc;
}

body.light-theme {
    /* 라이트 테마 */
    --bg-primary: #ffffff;
    --bg-secondary: #f3f4f6;
    --bg-tertiary: #e5e7eb;
    --text-primary: #1f2937;
    --accent-primary: #2563eb;
}
```

#### 적용된 색상 변수
| 변수명 | 다크 테마 | 라이트 테마 | 용도 |
|--------|-----------|-------------|------|
| `--bg-primary` | `#1e1e1e` | `#ffffff` | 메인 배경 |
| `--bg-secondary` | `#252526` | `#f3f4f6` | 사이드바 |
| `--bg-tertiary` | `#2d2d30` | `#e5e7eb` | 카드 |
| `--bg-hover` | `#3c3c3c` | `#d1d5db` | 호버 상태 |
| `--text-primary` | `#cccccc` | `#1f2937` | 본문 텍스트 |
| `--text-secondary` | `#8c8c8c` | `#6b7280` | 보조 텍스트 |
| `--accent-primary` | `#007acc` | `#2563eb` | 액센트 컬러 |
| `--accent-green` | `#10a37f` | `#059669` | 성공 |

#### 전환 애니메이션
```css
body {
    transition: background-color var(--transition-slow), 
                color var(--transition-slow);
}

.theme-toggle-btn:hover i {
    transform: rotate(20deg);
}
```

---

### 2.2 프로그레스 바 시스템 📊

#### 기능 개요
요약 생성 중 진행 상황을 실시간으로 표시

#### UI 구성
```html
<div class="loading-overlay" id="loadingOverlay">
    <div class="loading-content">
        <div class="spinner"></div>
        <p id="loadingText">강의 내용 분석 중…</p>
        <div class="progress-bar-container">
            <div class="progress-bar-fill" id="progressBarFill"></div>
        </div>
        <p class="progress-text" id="progressText">0%</p>
    </div>
</div>
```

#### 진행 단계
| 퍼센트 | 메시지 |
|--------|--------|
| 0% | 처리 시작 중... |
| 20% | 오디오 파일 업로드 중... |
| 40% | 음성 인식 처리 중... |
| 60% | 텍스트 변환 중... |
| 80% | AI 요약 생성 중... |
| 95% | 최종 처리 중... |
| 100% | 완료! |

#### Shimmer 효과
```css
.progress-bar-fill::after {
    content: '';
    position: absolute;
    background: linear-gradient(
        90deg,
        transparent,
        rgba(255, 255, 255, 0.3),
        transparent
    );
    animation: shimmer 1.5s infinite;
}

@keyframes shimmer {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(100%); }
}
```

#### JavaScript 구현
```javascript
function updateProgressBar(percent, text) {
    const progressFill = document.getElementById('progressBarFill');
    const progressText = document.getElementById('progressText');
    const loadingText = document.getElementById('loadingText');
    
    if (progressFill) {
        progressFill.style.width = `${percent}%`;
    }
    if (progressText) {
        progressText.textContent = `${Math.round(percent)}%`;
    }
    if (text && loadingText) {
        loadingText.textContent = text;
    }
}

function simulateProgress(duration = 10000) {
    const steps = [
        { percent: 20, text: '오디오 파일 업로드 중...' },
        { percent: 40, text: '음성 인식 처리 중...' },
        { percent: 60, text: '텍스트 변환 중...' },
        { percent: 80, text: 'AI 요약 생성 중...' },
        { percent: 95, text: '최종 처리 중...' }
    ];
    
    // ... 단계별 업데이트 로직
}
```

---

### 2.3 애니메이션 효과 강화 ✨

#### 탭 전환 페이드 효과
```css
.tab-content {
    opacity: 0;
    transform: translateY(10px);
    transition: opacity var(--transition-slow), 
                transform var(--transition-slow);
}

.tab-content.active {
    animation: fadeInUp 0.3s ease forwards;
}

@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
```

#### 모달 슬라이드 인
```css
.modal-content {
    animation: modalSlideIn 0.3s ease;
}

@keyframes modalSlideIn {
    from {
        transform: translateY(-20px) scale(0.95);
        opacity: 0;
    }
    to {
        transform: translateY(0) scale(1);
        opacity: 1;
    }
}
```

#### 버튼 클릭 효과
```javascript
function toggleTheme() {
    // ... 테마 전환 로직
    
    const btn = document.querySelector('.theme-toggle-btn');
    if (btn) {
        btn.style.transform = 'scale(0.9)';
        setTimeout(() => {
            btn.style.transform = 'scale(1)';
        }, 150);
    }
}
```

---

### 2.4 모바일 터치 제스처 📱

#### 기능 개요
모바일 환경에서 사이드바를 스와이프로 열고 닫을 수 있음

#### 제스처 동작
- **위로 스와이프** (↑): 사이드바 열기
- **아래로 스와이프** (↓): 사이드바 닫기
- **50px 이상 드래그** 시 토글
- **실시간 위치 추적**: 드래그 중 사이드바 따라감

#### UI 요소

##### 1. 모바일 햄버거 버튼
```javascript
function addMobileSidebarToggle() {
    const btn = document.createElement('button');
    btn.className = 'mobile-sidebar-toggle';
    btn.innerHTML = '<i class="fas fa-bars"></i>';
    btn.addEventListener('click', toggleMobileSidebar);
    document.body.appendChild(btn);
}
```

```css
.mobile-sidebar-toggle {
    display: none;
    position: fixed;
    bottom: 80px;
    right: 20px;
    width: 56px;
    height: 56px;
    background: var(--accent-primary);
    border-radius: 50%;
    z-index: 98;
}

@media (max-width: 768px) {
    .mobile-sidebar-toggle {
        display: flex;
    }
}
```

##### 2. 스와이프 인디케이터
```css
.swipe-indicator {
    position: absolute;
    top: 8px;
    left: 50%;
    transform: translateX(-50%);
    width: 40px;
    height: 4px;
    background: var(--text-secondary);
    border-radius: 2px;
    opacity: 0.5;
}
```

##### 3. 배경 오버레이
```javascript
const overlay = document.createElement('div');
overlay.className = 'mobile-sidebar-overlay';
overlay.addEventListener('click', closeMobileSidebar);
document.body.appendChild(overlay);
```

```css
.mobile-sidebar-overlay {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.5);
    z-index: 99;
    opacity: 0;
    pointer-events: none;
    transition: opacity var(--transition-normal);
}

.mobile-sidebar-overlay.active {
    opacity: 1;
    pointer-events: auto;
}
```

#### 터치 이벤트 핸들러

##### TouchStart
```javascript
function handleTouchStart(e) {
    if (window.innerWidth > 768) return;
    
    const touch = e.touches[0];
    touchStartY = touch.clientY;
    touchCurrentY = touch.clientY;
    isSidebarDragging = true;
    
    const sidebar = document.querySelector('.sidebar');
    sidebar.classList.add('touch-dragging');
}
```

##### TouchMove
```javascript
function handleTouchMove(e) {
    if (!isSidebarDragging || window.innerWidth > 768) return;
    
    const touch = e.touches[0];
    touchCurrentY = touch.clientY;
    const deltaY = touchCurrentY - touchStartY;
    
    const sidebar = document.querySelector('.sidebar');
    
    // 아래로 드래그 (닫기)
    if (deltaY > 0 && mobileSidebarOpen) {
        e.preventDefault();
        const translateY = Math.min(deltaY, window.innerHeight);
        sidebar.style.transform = `translateY(${translateY}px)`;
    }
    
    // 위로 드래그 (열기)
    if (deltaY < 0 && !mobileSidebarOpen) {
        e.preventDefault();
        const translateY = Math.max(0, window.innerHeight + deltaY);
        sidebar.style.transform = `translateY(${translateY}px)`;
    }
}
```

##### TouchEnd
```javascript
function handleTouchEnd(e) {
    if (!isSidebarDragging || window.innerWidth > 768) return;
    
    const deltaY = touchCurrentY - touchStartY;
    const sidebar = document.querySelector('.sidebar');
    
    sidebar.classList.remove('touch-dragging');
    sidebar.style.transform = '';
    
    // 50px 이상 드래그하면 토글
    if (Math.abs(deltaY) > 50) {
        if (deltaY > 0 && mobileSidebarOpen) {
            closeMobileSidebar();
        } else if (deltaY < 0 && !mobileSidebarOpen) {
            openMobileSidebar();
        }
    }
    
    isSidebarDragging = false;
}
```

#### 반응형 레이아웃
```css
@media (max-width: 768px) {
    .sidebar {
        position: fixed;
        left: 0;
        bottom: 0;
        width: 100%;
        height: 70vh;
        z-index: 100;
        transform: translateY(100%);
        transition: transform var(--transition-normal);
    }
    
    .sidebar.mobile-open {
        transform: translateY(0);
    }
}
```

#### 터치 디바이스 최적화
```css
@media (hover: none) and (pointer: coarse) {
    /* 터치 영역 최소 44x44px */
    .sidebar-tab,
    .control-btn,
    .action-btn,
    button {
        min-height: 44px;
        min-width: 44px;
    }
    
    .tree-node-content,
    .summary-item {
        padding: 12px 8px;
    }
}
```

---

## 3. 기술 구현

### 3.1 CSS 변수 시스템

#### 도입 배경
- 하드코딩된 색상 값 제거
- 테마 전환 용이
- 유지보수성 향상
- 일관된 디자인 시스템

#### 변경 전 (Before)
```css
.sidebar {
    background-color: #252526;
    border-right: 1px solid #3c3c3c;
}

.tab {
    color: #cccccc;
}

.tab:hover {
    background-color: #37373d;
}
```

#### 변경 후 (After)
```css
.sidebar {
    background-color: var(--bg-secondary);
    border-right: 1px solid var(--border-primary);
}

.tab {
    color: var(--text-primary);
}

.tab:hover {
    background-color: var(--bg-active);
}
```

#### 전체 변수 목록
```css
:root {
    /* 배경 색상 */
    --bg-primary: #1e1e1e;
    --bg-secondary: #252526;
    --bg-tertiary: #2d2d30;
    --bg-hover: #3c3c3c;
    --bg-active: #37373d;
    
    /* 텍스트 색상 */
    --text-primary: #cccccc;
    --text-secondary: #8c8c8c;
    --text-tertiary: #6a6a6a;
    --text-white: #ffffff;
    
    /* 테두리 색상 */
    --border-primary: #3c3c3c;
    --border-secondary: #3e3e42;
    
    /* 액센트 색상 */
    --accent-primary: #007acc;
    --accent-hover: #1177bb;
    --accent-green: #10a37f;
    --accent-green-hover: #0d8f6f;
    --accent-red: #ef4444;
    --accent-yellow: #dcb67a;
    
    /* 그림자 */
    --shadow-sm: 0 4px 12px rgba(0, 0, 0, 0.3);
    --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.5);
    
    /* 트랜지션 */
    --transition-fast: 0.15s ease;
    --transition-normal: 0.2s ease;
    --transition-slow: 0.3s ease;
}
```

### 3.2 전역 변수 추가

#### JavaScript 변수
```javascript
// 기존 변수
let mediaRecorder = null;
let recordedChunks = [];
let isRecording = false;
// ...

// 신규 추가 (모바일 터치 제스처용)
let touchStartY = 0;
let touchCurrentY = 0;
let isSidebarDragging = false;
let mobileSidebarOpen = false;
```

### 3.3 버전 관리

#### 버전 업데이트
```javascript
// 변경 전
// Version: 2.0.1 - Fixed savedFileNames scope issue

// 변경 후
// Version: 2.1.0 - Added theme toggle, progress bar, touch gestures
```

---

## 4. 코드 변경 사항

### 4.1 파일별 변경 내역

#### index.html
**추가된 요소**:
1. 테마 전환 버튼 섹션 (12줄)
2. 프로그레스 바 요소 (5줄)

**변경 줄 수**: +17줄

#### styles.css
**주요 변경 사항**:
1. CSS 변수 시스템 도입 (80줄)
2. 라이트 테마 정의 (30줄)
3. 테마 전환 버튼 스타일 (60줄)
4. 프로그레스 바 스타일 (50줄)
5. 모바일 반응형 개선 (120줄)
6. 터치 제스처 스타일 (80줄)
7. 애니메이션 키프레임 추가 (30줄)
8. 기존 하드코딩 색상 → CSS 변수 (200개 이상 변경)

**변경 줄 수**: +450줄, 수정 ~500줄

#### script.js
**새로운 함수**:
1. `toggleTheme()` - 테마 전환 (20줄)
2. `loadTheme()` - 테마 로드 (5줄)
3. `updateProgressBar(percent, text)` - 프로그레스 바 업데이트 (15줄)
4. `simulateProgress(duration)` - 진행률 시뮬레이션 (25줄)
5. `initTouchGestures()` - 터치 제스처 초기화 (20줄)
6. `handleTouchStart(e)` - 터치 시작 핸들러 (10줄)
7. `handleTouchMove(e)` - 터치 이동 핸들러 (25줄)
8. `handleTouchEnd(e)` - 터치 종료 핸들러 (20줄)
9. `addMobileSidebarToggle()` - 모바일 버튼 추가 (10줄)
10. `toggleMobileSidebar()` - 사이드바 토글 (5줄)
11. `openMobileSidebar()` - 사이드바 열기 (10줄)
12. `closeMobileSidebar()` - 사이드바 닫기 (10줄)

**수정된 함수**:
1. `showLoading(show)` - 프로그레스 바 통합 (15줄)
2. `DOMContentLoaded` 리스너 - 초기화 함수 추가 (3줄)

**변경 줄 수**: +183줄

### 4.2 총 변경 통계

| 파일 | 추가 | 수정 | 삭제 |
|------|------|------|------|
| index.html | +17 | 0 | 0 |
| styles.css | +450 | ~500 | 0 |
| script.js | +183 | ~20 | 0 |
| **합계** | **+650** | **~520** | **0** |

---

## 5. 테스트 및 검증

### 5.1 테마 전환 테스트

#### 테스트 케이스
| 번호 | 테스트 항목 | 결과 |
|------|------------|------|
| 1 | 다크 → 라이트 전환 | ✅ 통과 |
| 2 | 라이트 → 다크 전환 | ✅ 통과 |
| 3 | localStorage 저장 | ✅ 통과 |
| 4 | 페이지 새로고침 후 테마 유지 | ✅ 통과 |
| 5 | 모든 버튼 색상 변경 확인 | ✅ 통과 |
| 6 | 텍스트 가독성 확인 | ✅ 통과 |

#### 검증된 UI 요소
- ✅ 사이드바 배경/텍스트
- ✅ 탭 바 및 탭 버튼
- ✅ 녹음 버튼, 요약 버튼
- ✅ 파일 업로드 영역
- ✅ 폴더 선택 UI
- ✅ 카드 및 리스트 아이템
- ✅ 모달 및 오버레이
- ✅ 프로그레스 바

### 5.2 프로그레스 바 테스트

#### 테스트 케이스
| 번호 | 테스트 항목 | 결과 |
|------|------------|------|
| 1 | 0% → 100% 진행 | ✅ 통과 |
| 2 | 단계별 메시지 표시 | ✅ 통과 |
| 3 | Shimmer 애니메이션 | ✅ 통과 |
| 4 | 완료 후 자동 숨김 | ✅ 통과 |

### 5.3 모바일 터치 제스처 테스트

#### 테스트 환경
- iPhone 12 Pro (iOS 16)
- Samsung Galaxy S21 (Android 12)
- Chrome DevTools (Mobile Emulation)

#### 테스트 케이스
| 번호 | 테스트 항목 | 결과 |
|------|------------|------|
| 1 | 위로 스와이프 (사이드바 열기) | ✅ 통과 |
| 2 | 아래로 스와이프 (사이드바 닫기) | ✅ 통과 |
| 3 | 드래그 중 실시간 추적 | ✅ 통과 |
| 4 | 50px 미만 드래그 시 원위치 | ✅ 통과 |
| 5 | 햄버거 버튼 클릭 | ✅ 통과 |
| 6 | 오버레이 클릭 시 닫힘 | ✅ 통과 |
| 7 | 터치 영역 최소 44px | ✅ 통과 |

### 5.4 애니메이션 테스트

#### 테스트 케이스
| 번호 | 테스트 항목 | 결과 |
|------|------------|------|
| 1 | 탭 전환 페이드 효과 | ✅ 통과 |
| 2 | 모달 슬라이드 인 | ✅ 통과 |
| 3 | 버튼 클릭 스케일 효과 | ✅ 통과 |
| 4 | 프로그레스 바 Shimmer | ✅ 통과 |
| 5 | 60fps 유지 확인 | ✅ 통과 |

### 5.5 브라우저 호환성

| 브라우저 | 버전 | 테마 전환 | 터치 제스처 | 프로그레스 바 |
|---------|------|----------|------------|--------------|
| Chrome | 120+ | ✅ | ✅ | ✅ |
| Edge | 120+ | ✅ | ✅ | ✅ |
| Safari (iOS) | 16+ | ✅ | ✅ | ✅ |
| Samsung Internet | 20+ | ✅ | ✅ | ✅ |

---

## 6. 성과 및 개선점

### 6.1 주요 성과

#### UX 개선 지표
- ✅ **테마 선택권 제공**: 사용자 맞춤형 경험
- ✅ **진행 상황 가시성**: 불안감 해소
- ✅ **모바일 접근성**: 터치 제스처 지원
- ✅ **일관된 디자인**: CSS 변수 시스템

#### 코드 품질 향상
- ✅ **유지보수성**: CSS 변수로 중앙 관리
- ✅ **확장성**: 새로운 테마 추가 용이
- ✅ **재사용성**: 모듈화된 함수
- ✅ **가독성**: 명확한 함수명과 주석

#### 성능 최적화
- ✅ **CSS 트랜지션**: GPU 가속 활용
- ✅ **이벤트 최적화**: passive 이벤트 리스너
- ✅ **애니메이션**: transform 사용 (reflow 방지)

### 6.2 사용자 피드백 (예상)

#### 긍정적 피드백
- 👍 "다크/라이트 테마 전환이 편리합니다"
- 👍 "진행 상황을 볼 수 있어서 좋아요"
- 👍 "모바일에서 사용하기 편해졌어요"
- 👍 "애니메이션이 부드럽고 자연스럽습니다"

#### 개선 필요 사항
- ⚠️ Firefox/Safari 일부 기능 제한 (File System Access API)
- ⚠️ 모바일 가로 모드 최적화 필요
- ⚠️ 터치 제스처 튜토리얼 필요

### 6.3 배운 점

#### 기술적 학습
1. **CSS 변수 시스템**
   - `:root`와 클래스 기반 테마 전환
   - `var()` 함수 활용
   - 폴백 값 설정

2. **터치 이벤트**
   - `touchstart`, `touchmove`, `touchend`
   - passive 이벤트 리스너
   - 제스처 인식 알고리즘

3. **애니메이션 최적화**
   - `transform` vs `position` 성능 차이
   - `will-change` 속성
   - 60fps 유지 기법

4. **반응형 디자인**
   - 미디어 쿼리 전략
   - 모바일 퍼스트 vs 데스크톱 퍼스트
   - 터치 디바이스 감지

#### 디자인 원칙
1. **점진적 향상 (Progressive Enhancement)**
   - 기본 기능은 모든 브라우저에서 작동
   - 고급 기능은 지원되는 브라우저에서만

2. **일관성 (Consistency)**
   - 전체 UI에서 동일한 색상 체계
   - 통일된 트랜지션 속도

3. **접근성 (Accessibility)**
   - 충분한 색상 대비
   - 최소 터치 영역 44x44px
   - 키보드 네비게이션 지원

### 6.4 향후 개발 계획

#### 단기 목표 (1-2주)
- [ ] 커스텀 테마 생성 기능
- [ ] 다크/라이트/자동(시스템 설정 따름) 3가지 옵션
- [ ] 모바일 가로 모드 최적화
- [ ] 터치 제스처 튜토리얼 추가

#### 중기 목표 (1개월)
- [ ] 검색 기능 구현
- [ ] 태그 시스템
- [ ] 북마크/즐겨찾기
- [ ] PDF 내보내기

#### 장기 목표 (2-3개월)
- [ ] PWA 지원 (오프라인 모드)
- [ ] 다국어 지원 (i18n)
- [ ] 협업 기능 (공유, 댓글)
- [ ] 통계 대시보드

---

## 7. 결론

### 7.1 2주차 달성 목표

#### 계획 대비 달성률: **100%**

| 목표 | 상태 | 달성도 |
|------|------|--------|
| 다크/라이트 테마 전환 | ✅ 완료 | 100% |
| 애니메이션 효과 강화 | ✅ 완료 | 100% |
| 모바일 반응형 디자인 | ✅ 완료 | 100% |

### 7.2 핵심 개선 사항

#### Before (1주차)
- ❌ 다크 테마만 지원
- ❌ 정적인 UI
- ❌ 데스크톱 중심 설계
- ❌ 하드코딩된 색상 값

#### After (2주차)
- ✅ 다크/라이트 테마 자유 전환
- ✅ 부드러운 애니메이션
- ✅ 모바일 터치 제스처 지원
- ✅ CSS 변수 시스템

### 7.3 프로젝트 현황

#### 코드 통계
- **HTML**: ~417줄 (+17)
- **JavaScript**: ~3,793줄 (+183)
- **CSS**: ~2,643줄 (+450)
- **총 코드 라인**: ~6,853줄 (+650)

#### 기능 완성도
- **핵심 기능**: 100% ✅
- **UI/UX**: 95% ✅
- **모바일 최적화**: 90% ✅
- **접근성**: 85% 🔄

### 7.4 최종 평가

#### 강점
- ✅ 전문적인 VSCode 스타일 UI
- ✅ 완전한 로컬 파일 시스템 연동
- ✅ AI 요약 및 화자 구분
- ✅ 유연한 테마 시스템
- ✅ 모바일 친화적 인터페이스

#### 개선 필요
- 🔄 Firefox/Safari 브라우저 지원
- 🔄 코드 모듈화 (현재 단일 파일)
- 🔄 에러 핸들링 강화
- 🔄 검색 및 필터링 기능

### 7.5 다음 단계

**3주차 목표**: 기능 확장 및 사용성 개선
- 검색 기능 구현
- 태그 시스템
- 통계 대시보드
- 코드 리팩토링

